[project]
name = "ai-benchmark"
version = "0.1.0"
description = "RAG retrieval benchmark — COMP702 MSc project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths   = ["tests"]
pythonpath  = ["src"]
addopts     = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py310"
