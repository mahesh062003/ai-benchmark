"""Answer generation via a local Ollama model.

This stage is optional. Retrieval benchmarking runs to completion without it,
so a machine with no GPU and no Ollama server can still reproduce every
retrieval result.
"""
